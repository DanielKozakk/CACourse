W głównym katalogu zrób :
sudo docker-compose build

docker-compose up -d
docker exec -it -u dev sf5_php bash
cd /home/wwwroot/sf5
composer create-project symfony/skeleton my-temp-folder

cp -Rf /home/wwwroot/sf5/my-temp-folder/. .
rm -Rf /home/wwwroot/sf5/my-temp-folder

Wychodząc w terminalu na własny komputer:
sudo chmod -R 777 var/cache
sudo chmod -R 777 var/log
